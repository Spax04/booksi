const STORE_SLICES = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials',
    AUTH: 'auth'
}
const PERSIST_KEYS = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials',
    AUTH: 'auth'

}

const CONTROLLERS = {
    LOGIN: 'login',
}

export{
    STORE_SLICES,
    PERSIST_KEYS,
    CONTROLLERS
}