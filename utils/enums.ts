const STORE_SLICES = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials'
}
const PERSIST_KEYS = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials'
}
export{
    STORE_SLICES,
    PERSIST_KEYS
}