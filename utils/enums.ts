const STORE_SLICES = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials'
}
const PERSIST_KEYS = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials'
}

const BACKGROUND_TASKS_IDENTIFIERS = {
    TEXT_CONVERSION: 'textConversion',
    AUDIO_CONVERSIONS: 'audioConversions'
}

export{
    STORE_SLICES,
    PERSIST_KEYS,
    BACKGROUND_TASKS_IDENTIFIERS
}