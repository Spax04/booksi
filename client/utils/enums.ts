const STORE_SLICES = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials',
    USER: 'user'
}
const PERSIST_KEYS = {
    DOCUMENTS: 'documents',
    CREDENTIALS: 'credentials',
    USER: 'user'

}

export{
    STORE_SLICES,
    PERSIST_KEYS,
}